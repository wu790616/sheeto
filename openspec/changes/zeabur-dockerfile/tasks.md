## 1. Docker Configuration

- [ ] 1.1 Create the multi-stage `Dockerfile` in the project root to compile Vite assets and copy them into the `zeabur/caddy-static` container.

## 2. Verification

- [ ] 2.1 Verify the Docker image builds successfully.
- [ ] 2.2 Verify the containerized server starts up and serves the React application.
