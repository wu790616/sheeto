## 1. Nginx Configuration

- [ ] 1.1 Create the `nginx.conf` file in the project root to listen on port 80 and fallback unmatched URL routes to `index.html`.

## 2. Docker Configuration

- [ ] 2.1 Create the multi-stage `Dockerfile` in the project root to compile Vite assets and copy them into the Nginx Alpine container.

## 3. Verification

- [ ] 3.1 Verify the Docker image builds successfully.
- [ ] 3.2 Verify the containerized server starts up and serves the React application.
